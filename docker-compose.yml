version: "3.9"

services:
  api:
    build: .
    container_name: spycats-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    command: >
      sh -c "
      alembic upgrade head &&
      uvicorn app.main:app --host 0.0.0.0 --port 8000
      "
